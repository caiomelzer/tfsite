<!DOCTYPE html>
<html lang="en">
<head>
    <%- include partials/top-site.ejs %>
    
</head><!--/head-->
<body>
    <%- include partials/header.ejs %>
    
    <section id="title" class="emerald">
        <div class="container">
            <div class="row">
                <div class="col-sm-6">
                    <h1><%= __('Games') %></h1>
                    <!-- <p>Pellentesque habitant morbi tristique senectus et netus et malesuada</p> -->
                </div>
                <div class="col-sm-6">
                    <ul class="breadcrumb pull-right">
                        <li><a href="/">Home</a></li>
                        <li class="active"><%= __('Games') %></li>
                    </ul>
                </div>
            </div>
        </div>
    </section><!--/#title-->     

    <section id="registration" class="container">
        <div class="container">
            <%- include partials/alert.ejs %>
            

            <div class="row">
                <h2 class="col-sm-12 games-title"><%= __('Last Games') %></h2>
                <ul class="games-list col-sm-12">
                <% if(games_old && games_old.length > 0){ %>
                    <% for(var i=0;i<games_old.length;i++){ %>
                    <li class="col-sm-3 games-item" data-id="<%= games_old[i].game_id %>">
                        <div class="row">
                            <span class="team col-sm-5">
                                <%= games_old[i].alias_a %>
                                <img src="/images/uploads/<%= games_old[i].logo_a %>" />
                            </span>
                            <span class="versus col-sm-2">VS</span>
                            <span class="team col-sm-5">
                                <%= games_old[i].alias_b%>
                                <img src="/images/uploads/<%= games_old[i].logo_b %>" />

                            </span>
                        </div>  
                        <div class="row">  
                            <span class="place col-sm-12">
                                <p><%= games_old[i].name %></p>
                            </span>
                        </div>
                        <div class="row"> 
                        <div datetime="<%= games_old[i].date %>" class="col-sm-12 time">
                            <div class="row">
                                <div class="col-sm-5"> 
                                    <span class="date"></span> 
                                    <span class="hour"></span>
                                </div>
                                <div class="col-sm-7"> 
                                    <div class="result">
                                    <%= games_old[i].goals %> - <%= games_old[i].goals_against %>
                                    </div>
                                </div>
                            </div>
                        </div>
                        </div>
                    </li>
                    <% } %> 
                    
                    <% if(games_old.length > 6){ %>
                    <li class="col-sm-3 games-item games-see-more" >
                        <a href="/jogos/anteriores"><%= __('See more') %></a>
                    </li>
                    <% } %>
                    
                <% } else { %>
                    <p><%= __('No games to show') %></p>
                <% } %>    
                </ul>
            </div>

            <div class="row">
                <h2 class="col-sm-12 games-title"><%= __('Up comming') %></h2>
                <ul class="games-list col-sm-12">
                <% if(games && games.length > 0){ %>
                    <% for(var i=0;i<games.length;i++){ %>
                    <li class="col-sm-3 games-item" data-id="<%= games[i].game_id %>">
                        <div class="row">
                            <span class="team col-sm-5">
                                <%= games[i].alias_a %>
                                <img src="/images/uploads/<%= games[i].logo_a %>" />
                            </span>
                            <span class="versus col-sm-2">VS</span>
                            <span class="team col-sm-5">
                                <%= games[i].alias_b%>
                                <img src="/images/uploads/<%= games[i].logo_b %>" />
                            </span>
                        </div>  
                        <div class="row">  
                            <span class="place col-sm-12">
                                <p><%= games[i].name %></p>
                            </span>
                        </div>
                        <div class="row"> 
                        <div datetime="<%= games[i].date %>" class="col-sm-12 time">
                            <div class="row">
                                <div class="col-sm-5"> 
                                    <span class="date"></span> 
                                    <span class="hour"></span>
                                </div>
                                <div class="col-sm-7"> 
                                    <div class="result">
                                    <%= games[i].goals %> - <%= games[i].goals_against %>
                                    </div>
                                </div>
                            </div>
                        </div>
                        </div>
                    </li>
                    <% } %>
                    <% if(games.length > 6){ %>
                    <li class="col-sm-3 games-item games-see-more" >
                        <a href="/jogos/proximos"><%= __('See more') %></a>
                    </li>    
                    <% } %>
                    
                <% } else { %>
                    <p><%= __('No games to show') %></p>
                <% } %>    
                </ul>
            </div>
        </div>
    </section><!--/#registration-->   
    <%- include partials/footer.ejs %>
    <script src="/js/util.js"></script>
    <script src="/js/validator.js"></script>
    <script src="/js/view/jogos.js"></script>

</body>
</html>

